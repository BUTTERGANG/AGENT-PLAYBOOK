# {{FRAMEWORK_NAME}} HAS CHANGED

Read the exact versioned docs at {{DOCS_URL}} before writing any code.

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data.